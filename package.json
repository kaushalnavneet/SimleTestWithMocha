{
  "name": "HelloWorldNodeApp",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "test": "mocha test/test --reporter mocha-junit-reporter --reporter-options mochaFile=./testResult/test-result.xml && mocha test/test --reporter mocha-junit-reporter --reporter-options mochaFile=./testResult/test-result1.xml",
    "coverage": "istanbul cover ./node_modules/.bin/_mocha test/test.js"
  },
  "dependencies": {
    "express": "4.13.x",
    "cfenv": "1.0.x"
  },
  "repository": {},
  "engines": {
    "node": "10.x.x"
  },
  "devDependencies": {
    "istanbul": "^0.4.5",
    "mocha": "6.1.4",
    "mocha-junit-reporter": "1.22.0",
    "nyc": "^13.1.0"
  }
}
